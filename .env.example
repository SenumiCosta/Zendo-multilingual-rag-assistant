# Environment variables for the Zendo multilingual RAG assistant.
# Copy to `.env` and fill in HUGGINGFACE_TOKEN.

# --- LLM (HuggingFace Inference API) ---
# Pick any "warm" chat model:
#   https://huggingface.co/models?inference=warm&pipeline_tag=text-generation
# Known-working examples (as of 2026; availability changes):
#   Qwen/Qwen2.5-7B-Instruct         (ungated, multilingual)
#   HuggingFaceH4/zephyr-7b-beta     (ungated)
#   meta-llama/Llama-3.1-8B-Instruct (gated — accept license first)
MODEL_NAME=Qwen/Qwen2.5-7B-Instruct

# Get a free Read token: https://huggingface.co/settings/tokens
HUGGINGFACE_TOKEN=your_token_here

# Optional: force a specific Inference Provider when auto-routing fails.
# Leave blank to let HuggingFace pick one.
# Examples: hf-inference, together, fireworks-ai, cerebras, nebius
# LLM_PROVIDER=

# --- Embeddings ---
# BGE-M3: multilingual (100+ langs incl. Sinhala), 8K context, 1024-dim.
# Alternatives: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 (fast, 384-dim)
EMBEDDING_MODEL=BAAI/bge-m3

# --- Paths ---
VECTOR_DB_PATH=./data/vector_db
DOCUMENTS_PATH=./data/documents

# --- Agentic mode ---
# Set to 1 to route 'complex' and 'relational' queries through the
# LangGraph agentic loop (plan → retrieve → critic → re-retrieve → answer).
# Costs 2-4 extra LLM calls per query; much better on multi-hop questions.
# AGENTIC_MODE=1

# --- Multimodal (Gemini Embedding 2) ---
# Free key at https://aistudio.google.com/apikey. When set, the upload
# UI exposes a "multimodal" checkbox that also embeds rendered PDF pages
# into a 3072-dim image+text vector space. Enables the "Image search" tab.
# GOOGLE_API_KEY=your_google_api_key_here
